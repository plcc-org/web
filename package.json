{
  "name": "plcc-web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "ASTRO_DEV=1 astro dev",
    "dev:container": "container run --name plcc-dev --detach --rm --env ASTRO_CONTAINER_DEV=1 --publish 4321:4321 --mount type=bind,source=\"$PWD\",target=/workspace --tmpfs /workspace/node_modules --workdir /workspace docker.io/library/node:lts sh -lc \"npm ci && npm run dev -- --host 0.0.0.0 --port 4321\"",
    "dev:container:stop": "container stop plcc-dev",
    "_comment_clean:vite": "Vite's SSR dep cache goes stale across Astro/adapter upgrades and surfaces as 'The file does not exist at node_modules/.vite/deps_ssr/...'. Anything that loads the Astro config through Vite hits it, so both `build` and `check` clear it first rather than each carrying its own copy of the rm.",
    "clean:vite": "rm -rf node_modules/.vite",
    "_comment_clean": "Every derived directory in one place, for when a build goes strange. All four are gitignored and regenerate on the next build.",
    "clean": "rm -rf node_modules/.vite node_modules/.astro .astro dist",
    "_comment_prebuild": "Clears the stale dep cache, then generates public/_redirects from the short-links collection so astro build copies it into the deploy.",
    "prebuild": "npm run clean:vite && node scripts/generate-redirects.mjs",
    "_comment_build": "There is no plain `astro build` any more: getStaticPaths queries the CMS, so the build must run inside `tinacms build`, which starts the data layer for its duration. This is `build` rather than a separate `build:tina` on purpose — Cloudflare's build command lives in a dashboard the repo can't assert, and when it said `npm run build` the deploy failed on the unresolvable generated client. The correct build has to be the one behind the obvious name. NODE_ENV=production is also not redundant: `tinacms build` sets NODE_ENV=development for the command it wraps, which makes import.meta.env.PROD false and silently flips both PROD branches we have — events fall back to the curated list instead of the Church Center snapshot, and draft pages get published.",
    "build": "tinacms build --local --skip-cloud-checks -c \"NODE_ENV=production astro build\"",
    "postbuild": "node scripts/prune-dist.mjs && node scripts/prune-admin.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "precheck": "npm run clean:vite",
    "check": "astro check",
    "lint:css": "stylelint \"src/**/*.{css,astro}\"",
    "lint:css:fix": "stylelint --fix \"src/**/*.{css,astro}\"",
    "test": "vitest run",
    "test:site": "node scripts/check-site.mjs",
    "scrape:events": "node scripts/scrape-events.mjs",
    "generate:social-card": "node scripts/generate-social-card.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev:tina": "ASTRO_DEV=1 tinacms dev -c \"astro dev\"",
    "_comment_compare": "Regression check for the Tina migration: renders both builds down to what a reader sees and diffs them. Needs a built baseline in the sibling worktree — see spike/compare-builds.mjs. `compare:serve` puts the two builds on adjacent ports for a side-by-side look.",
    "compare": "node spike/compare-builds.mjs",
    "compare:serve": "node spike/serve-builds.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^6.0.3",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/fraunces": "5.2.9",
    "@fontsource-variable/instrument-sans": "5.2.8",
    "astro": "^6.4.8",
    "marked": "^18.0.5",
    "marked-smartypants": "^1.1.12",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^13.7.0",
    "@tinacms/astro": "^0.6.1",
    "@tinacms/cli": "^2.5.6",
    "@types/node": "^25.9.3",
    "playwright": "^1.61.1",
    "postcss-html": "^1.8.1",
    "prettier": "^3.8.4",
    "prettier-plugin-astro": "^0.14.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "sharp": "^0.34.5",
    "stylelint": "^17.14.1",
    "tinacms": "^3.11.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.102.0"
  }
}
