{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "types": ["astro/client", "node"]
  },
  "include": [".astro/types.d.ts", "**/*"],
  "// spike-note": [
    "tina/config.ts is excluded because type-checking it runs the compiler out of memory:",
    "defineConfig from `tinacms` pulls in a type graph large enough that `astro check` dies",
    "with 'Ineffective mark-compacts near heap limit', even at --max-old-space-size=4096.",
    "config.prebuild.jsx is the generated copy of the same thing. Everything else under",
    "tina/ is checked. public/admin/ is the built admin SPA \u2014 megabytes of vendor bundles."
  ],
  "exclude": ["dist", "public/admin", "tina/config.ts", "tina/__generated__/config.prebuild.jsx"]
}
