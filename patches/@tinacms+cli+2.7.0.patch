diff --git a/node_modules/@tinacms/cli/dist/index.js b/node_modules/@tinacms/cli/dist/index.js
index 4ddfbc7..68ef9df 100644
--- a/node_modules/@tinacms/cli/dist/index.js
+++ b/node_modules/@tinacms/cli/dist/index.js
@@ -4191,7 +4191,13 @@ ${dangerText(e.message)}
         });
       }
     }
-    await buildProductionSpa(configManager, database, codegen2.productionUrl);
+    if (process.env.TINA_PUBLISH_ADMIN === "true") {
+      await buildProductionSpa(configManager, database, codegen2.productionUrl);
+    } else {
+      logger.info(
+        "Skipping the admin SPA bundle (TINA_PUBLISH_ADMIN is not 'true') — see patches/ and scripts/prune-admin.mjs."
+      );
+    }
     await fs11.outputFile(
       configManager.outputGitignorePath,
       "index.html\nassets/"
